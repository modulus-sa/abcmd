containers:
  testing:
    setup:
      - !Alpine v3.6
      - !Install
        - python3
      - !Py3Requirements requirements-tests.txt

commands:
  py36-test: !Command
    container: testing
    run: pytest
  py36-watch: !Command
    container: testing
    run: pytest-watch
